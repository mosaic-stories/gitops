# Production Environment Configuration
# Updated automatically by GitHub Actions on merge to main

global:
  imageTag: "2681e7c" # Will be set to SHA (e.g., "abc1234") by CI/CD
  environment: prod
web:
  replicaCount: 2
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
coreApi:
  replicaCount: 3
  autoscaling:
    enabled: true
    minReplicas: 3
    maxReplicas: 20
